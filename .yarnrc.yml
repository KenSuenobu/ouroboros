nodeLinker: node-modules

enableGlobalCache: false

# Keep zipped package cache in repo (committed via Yarn's "zero installs" pattern is OFF;
# .gitignore excludes .yarn/cache so each clone re-fetches into the repo-local cache).
